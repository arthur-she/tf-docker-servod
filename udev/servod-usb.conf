# Transient state for servod-usb-handler. Recreated empty on each boot,
# before systemd-udevd processes cold-plug events, so stale kernel->serial
# mappings from a previous boot can't be consulted.
D /run/servod-usb 0755 root root - -
